import leftH4001 from './hands/leftH4001.jpg';
import leftH4002 from './hands/leftH4002.jpg';
import rightH4001 from './hands/rightH4001.jpg';
import rightH4002 from './hands/rightH4002.jpg';

const imageMap = {
  leftH4001,
  leftH4002,
  rightH4001,
  rightH4002,
};

// Map images based on left or right condition
export const mapImages = exercise => {
  // const chosenKey = game rule (confidential)

  for (const [key, image] of Object.entries(imageMap)) {
    if (key.includes(chosenKey)) {
      // const imageObject = game rule (confidential)
    }
  }

  return imageList;
};

// game rule (confidential)

export default allLeft.concat(allRight);
